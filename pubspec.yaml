name: with_animation
description: SwiftUI-style declarative animation primitives for Flutter.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.1
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
